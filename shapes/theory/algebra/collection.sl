shape theory.algebra.collection : theory.algebra.emergent-existence {
  type: theorem
  layer: 0
  """
// Theorem 16.20 (Emergent Collection).
// If shapes A_1...A_n persist and are structurally identifiable,
// then the collection containing exactly those shapes emergently
// exists.
//
// Collection geometry holds members in structural isolation:
// the anti-forcing shape. Members are held, not pushed.
// No internal forcing means no internal resolution pressure.
//
// Structure/character applies: two collections with the same
// number of slots have the same structure. They differ in
// character (which shapes occupy slots). This is the structural
// basis for extensionality.
//
// Extends to infinite collections via coherent generation rules.
//
// Derives from: theory.algebra.emergent-existence
  """
}
